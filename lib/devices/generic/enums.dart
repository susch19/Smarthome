enum SpecialType { none, right }

enum SpecialDetailType {
  none,
  target,
  current,
}

enum EditType {
  button,
  raisedButton,
  floatingActionButton,
  iconButton,
  toggle,
  dropdown,
  slider,
  input,
  icon /*colorpicker*/,
  text
}
